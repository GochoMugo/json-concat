"use strict"


# npm-installed modules
shelljs = require "shelljs"


# module variables
cmd = "../dist/cmd.js "


describe "json-concat command"

    it "ignores if only one arg is passed"

    it "assumes last arg is the output file"
